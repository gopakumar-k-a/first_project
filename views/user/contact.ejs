<%- include('header.ejs') %>
    <!-- Header Area End Here -->
    <!-- Begin Li's Breadcrumb Area -->
    <div class="breadcrumb-area">
        <div class="container">
            <div class="breadcrumb-content">
                <ul>
                    <li><a href="/">Home</a></li>
                    <li class="active">Contact</li>
                </ul>
            </div>
        </div>
    </div>
    <!-- Li's Breadcrumb Area End Here -->
    <!-- Begin Contact Main Page Area -->
    <div class="contact-main-page mt-60 mb-40 mb-md-40 mb-sm-40 mb-xs-40">
        <div class="container mb-60">

        </div>
        <div class="container">
            <div class="row">
                <div class="col-lg-5 offset-lg-1 col-md-12 order-1 order-lg-2">
                    <div class="contact-page-side-content">
                        <h3 class="contact-page-title">Contact Us</h3>
                        <p class="contact-page-message mb-25"><strong>"
                            Your Voice, Our Priority":
                            At JERSYMAN,</strong> we firmly believe that the foundation of a successful business lies in
                            listening to our customers. 'Your Voice, Our Priority' isn't just a slogan; it's a
                            commitment woven into the fabric of our company ethos. </p>
                        <div class="single-contact-block">
                            <h4><i class="fa fa-fax"></i> Address</h4>
                            <p> Kochi,Kerala,India

                            </p>
                        </div>
                        <div class="single-contact-block">
                            <h4><i class="fa fa-phone"></i> Phone</h4>
                            <p>Mobile: 9388867491</p>
                           
                        </div>
                        <div class="single-contact-block last-child">
                            <h4><i class="fa fa-envelope-o"></i> Email</h4>
                            <p>jersymanauth@gmail.com</p>
                            <p>gopak9145@gmail.com</p>
                        </div>
                    </div>
                </div>






                <div class="col-lg-6 col-md-12 order-2 order-lg-1">
                    <div class="contact-form-content pt-sm-55 pt-xs-55">

                        <% if (message) { %>
                            <p class="text-success text-center">Email send Success fully</p>
                            <% } %>
                                <h3 class="contact-page-title">Tell Us Your Message</h3>
                                <div class="contact-form">
                                    <form id="contact-form" action="/contact" action="post">
                                        <div class="form-group">
                                            <label>Your Name <span class="required">*</span></label>
                                            <input type="text" name="customerName" id="customername" required>
                                            <li class="text-danger" id="customerNameErr"></li>
                                        </div>
                                        <div class="form-group">
                                            <label>Your Email <span class="required">*</span></label>
                                            <input type="email" name="customerEmail" id="customerEmail" required>
                                            <li class="text-danger" id="customerEmailErr"></li>

                                        </div>
                                        <div class="form-group">
                                            <label>Subject<span class="required">*</span></label>
                                            <input type="text" name="contactSubject" id="contactSubject" required>
                                            <li class="text-danger" id="contactSubjectErr"></li>

                                        </div>
                                        <div class="form-group mb-30">
                                            <label>Your Message<span class="required">*</span></label>
                                            <textarea name="contactMessage" id="contactMessage" required></textarea>
                                            <li class="text-danger" id="contactMessageErr"></li>
                                        </div>
                                        <div class="form-group">
                                            <button type="submit" value="submit" id="submit" class="li-btn-3"
                                                name="submit">send</button>
                                        </div>
                                    </form>
                                </div>
                                <!-- <script>
                            function validateForm() {
                                const customerName = document.getElementById('customername').value.trim();
                                const customerEmail = document.getElementById('customerEmail').value.trim();
                                const contactSubject = document.getElementById('contactSubject').value.trim();
                                const contactMessage = document.getElementById('contactMessage').value.trim();
                                console.log('Validating form...');
                                // Reset previous error messages
                                resetErrorMessages();

                                // Check if all fields are filled
                                if (customerName === '') {
                                    showError('customerNameErr', 'Please enter your name.');
                                    return false;
                                }

                                if (customerEmail === '') {
                                    showError('customerEmailErr', 'Please enter your email.');
                                    return false;
                                }

                                if (contactSubject === '') {
                                    showError('contactSubjectErr', 'Please enter a subject.');
                                    return false;
                                }

                                if (contactMessage === '') {
                                    showError('contactMessageErr', 'Please enter your message.');
                                    return false;
                                }
                                console.log('Validation passed');
                                return true;  // Validation passed
                                function showError(elementId, errorMessage) {
                                    const errorElement = document.getElementById(elementId);
                                    errorElement.textContent = errorMessage;
                                }

                            }

                            function resetErrorMessages() {
                                const errorElements = document.querySelectorAll('.text-danger');
                                errorElements.forEach(element => {
                                    element.textContent = '';
                                });
                            }
                            document.getElementById('contact-form').addEventListener('submit', function (event) {
                                event.preventDefault();
                                // Client-side validation
                                // const validationTest = validateForm()
                            

                                   
                                    const formData = new FormData(this);

                                    const jsonData = {};
                                    formData.forEach((value, key) => {
                                        jsonData[key] = value;
                                    });

                                    fetch('/contact', {
                                        method: 'POST',
                                        headers: {
                                            'Content-Type': 'application/json',
                                        },
                                        body:  JSON.stringify(jsonData),

                                    })
                                        .then(response => response.json())
                                        .then((data) => {
                                            if (data.success) {
                                                Swal.fire('Submitted', 'Your response is now Submitted.', 'success');
                                                return 0
                                                // setTimeout(() => {
                                                //     window.location.reload()
                                                // }, 1500)
                                            }
                                            if(data.failed){
                                                console.log(data.failed);
                                            }
                                        })
                                        .catch(error => {

                                        })
                                

                            });

                        </script> -->
                                <!-- <p class="form-messege"></p> -->
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- Contact Main Page Area End Here -->
    <%- include('footer.ejs') %>